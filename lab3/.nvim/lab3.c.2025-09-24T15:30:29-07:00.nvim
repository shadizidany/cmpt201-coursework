#define _POSIX_C_Source 200809L
#include <stdio.h>
#include <stdlib.h>

int main() {
  size_t len = 0;
  char *line[5];
  while (printf("Enter input: ") &&
         getline(&(line+i), &len, stdin) != -1) {
    break;
  }
  return 0;
}
